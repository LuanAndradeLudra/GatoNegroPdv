# Desenvolvimento local com `docker compose up` (Postgres exposto em 127.0.0.1:5433 no host)
DATABASE_URL="postgresql://gatonegro:gatonegro@127.0.0.1:5433/gatonegro?schema=public"

JWT_SECRET="change-me-in-production-use-long-random-string"
API_PORT=3001

# Só necessário se o servidor deve escutar em todas as interfaces (ex.: Docker)
# HOST=0.0.0.0
